# import * from "./generated/prisma.graphql"

type Mutation {
  signup(
    name: String!
    email: String!
    password: String!
    location: String
    disorders: [Disorder]
  ): User!
}

type Query {
  users: [User]!
}
